# Repo-level files that are not dotfiles
CODE_OF_CONDUCT.md
CONTRIBUTING.md
README.md
UNLICENSE
install.sh

# NOTE: patterns match TARGET paths (the deployed ~/… path), not source
# filenames — so no dot_ prefix and no .tmpl/executable_/run_once_ attributes.

# Optional features — excluded unless opted in via chezmoi init prompts
{{ if not .alt_tools }}.config/bat{{ end }}
{{ if not .alt_tools }}.config/eza{{ end }}
{{ if not .bitwarden }}.local/bin/bw-vault{{ end }}
{{ if not .macos_utils }}.hammerspoon{{ end }}
{{ if not .touch_id_sudo }}.chezmoiscripts/enable-touchid-sudo.sh{{ end }}

# sheldon lock file and static cache are generated at runtime
.config/sheldon/plugins.lock
.zsh/sheldon.zsh

# tool init files generated by run_onchange_generate-tool-inits.sh or mnt()
.zsh/zoxide_init.zsh
.zsh/carapace_init.zsh
.zsh/mise_init.zsh
.zsh/atuin_init.zsh
.zsh/direnv_init.zsh
.zsh/navi_widget.zsh

# completions generated by run_onchange_generate-tool-inits.sh or mnt()
.zsh/completions

# zsh compiled cache files
.zcompdump*
.zsh/**/*.zwc
.zshrc.zwc
