[settings]
gpg_verify = false

[tools]
# Runtime versions managed globally by mise.
# Pin versions per-project by adding a .mise.toml to the project root.
uv   = "latest"
node = "lts"
ruff = "latest"
{{ if .java -}}
java = "temurin-21"
{{ end -}}
