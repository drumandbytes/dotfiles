# Seeded once by chezmoi (create_), then owned at runtime by ~/.local/bin/sync-theme,
# which flips `skin:` to match the macOS appearance. Skins themselves come from the
# catppuccin/k9s archive in .chezmoiexternal.toml.tmpl.
k9s:
  ui:
    skin: catppuccin-macchiato
